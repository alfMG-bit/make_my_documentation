#config #auth #controllers #models #laravel-blade #Laravel 

Primeramente, cuando se busca hacer autenticaciones son paquetes de inicio, es bueno tener listas las plantillas a utlizar

Primero se agregan los ``guards`` y los ``providers`` al archivo ``Config/auth.php``. En este caso se agregan los que se vayan a usar además de ``user`` (trabajado por ``web``).